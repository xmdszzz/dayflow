## Agent skills

### Issue tracker

Issues 以本地 Markdown 文件存储在 `.scratch/<feature-slug>/` 下。详见 `docs/agents/issue-tracker.md`。

### Triage labels

使用标准英文标签：`needs-triage` / `needs-info` / `ready-for-agent` / `ready-for-human` / `wontfix`。详见 `docs/agents/triage-labels.md`。

### Domain docs

单上下文布局 — 根目录 `CONTEXT.md` + `docs/adr/`。详见 `docs/agents/domain.md`。
